<div class="inline">
  <h1>All Recipes</h1>
  <%= link_to "+ Add New", new_meal_path, style: "float:right" %>
</div>



<div class="flex-end">
    <% @meals.each do |meal| %>
    <div class="recipe">
      <%= link_to meal.name, meal_path(meal) %>
      <div class="preview" style="background-image: url(<%= meal.photo_url %>)"></div>
    </div>
    <% end %>
</div>
